// Script per la gestione dei preferiti
document.addEventListener('DOMContentLoaded', function() {
    console.log('Script dei preferiti caricato!');
    
    // Assicuriamoci che il pulsante originale sia visibile
    const originalButton = document.querySelector('.favorites-tab');
    if (originalButton) {
        originalButton.style.display = 'flex';
    }
    
    // Non creiamo il pulsante rettangolare
}); 